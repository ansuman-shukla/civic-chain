{
  "apiConfig": {
    "baseUrl": "http://localhost:3001/api",
    "endpoints": {
      "users": "/users",
      "grievances": "/grievances",
      "health": "/health"
    },
    "timeout": 10000,
    "retryAttempts": 3,
    "retryDelay": 1000
  },
  "options": {
    "batchSize": 10,
    "delayBetweenRequests": 100,
    "verbose": true
  }
}
